skills:
  - id: weather
    specificity: high
    sentences:
      - id: current
        captures:
          - id: where
            type: string

  - id: timer
    specificity: high
    sentences:
      - id: set
        captures:
          - id: duration
            type: duration
          - id: name
            type: string
      - id: cancel
        captures:
          - id: name
            type: string
      - id: query
        captures:
          - id: name
            type: string

  - id: current_time
    specificity: high
    sentences:
      - id: query
