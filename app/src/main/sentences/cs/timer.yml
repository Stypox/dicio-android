set:
  - časovač|(pípni za) .duration.
  - nastav|vytvoř|zapni|pusť časovač (na? .duration. (s názvem .name.)?)|((s názvem) .name. na .duration.)

cancel:
  - zastav|zruš|vypni|stop (.name.|(časovač (s názvem)? .name.))

query:
  - kdy skončí|vyprší|(bude u konce) časovac? (s názvem)? .name.
